db:
  image: postgres
  environment:
    - POSTGRES_PASSWORD=100shapes_shapes
  volumes:
    - /var/lib/postgresql/data
web:
  build: .
  environment:
    - SECRET='temp-key'
    - VIRTUAL_HOST='dockerhost'
  ports:
    - "80:80"
  volumes:
    - /home/app/public
  links:
    - db